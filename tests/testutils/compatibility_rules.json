{
  "drivers": {
    "postgres": {
      "rules": []
    },
    "oracle": {
      "rules": []
    },
    "mysql": {
      "rules": [
        {
          "data_types": [
            "ucs2",
            "utf16le",
            "latin1"
          ],
          "exclude_below": "v0.7.2",
          "note": "#940: non-UTF-8 charset bytes reach the writer as invalid UTF-8; gRPC marshal fails, retry backoff looks like a hang"
        },
        {
          "data_types": [
            "set"
          ],
          "assert_value_from": "v0.7.2",
          "note": "M1: SET columns emitted the numeric bitmask on the binlog path before #940"
        },
        {
          "data_types": [
            "enum"
          ],
          "assert_value_from": "v0.3.9",
          "note": "M2: ENUM serialization changed"
        },
        {
          "data_types": [
            "decimal",
            "numeric"
          ],
          "assert_value_from": "v0.3.7",
          "note": "M3: DECIMAL/NUMERIC round-tripped through float32"
        }
      ]
    },
    "mongodb": {
      "rules": [
        {
          "data_types": [
            "regex"
          ],
          "assert_value_from": "v0.3.14",
          "note": "G1: BSON regex serialized with Go field names, not lowercase keys, until #657"
        }
      ]
    },
    "mssql": {
      "min_baseline": "v0.3.15",
      "note": "first release carrying the driver"
    },
    "kafka": {
      "min_baseline": "v0.3.16",
      "note": "#735 (v0.3.12) added decoder.UseNumber() but the flattener only learned json.Number in #796 (v0.3.16); pre-#735 numbers all typed double against today's catalog",
      "variants": {
        "json": {},
        "avro": {}  
      }
    },
    "db2": {
      "min_baseline": "v0.3.14",
      "note": "first release carrying the driver; refine after the first sweep if early db2 images are missing from the registry",
      "rules": [
        {
          "data_types": [
            "decfloat"
          ],
          "assert_value_from": "v0.7.6",
          "note": "#936: ReformatValue rendered a float into a String column with %d"
        }
      ]
    },
    "s3": {
      "min_baseline": "v0.9.2",
      "note": "first release carrying the driver",
      "rules": [
        {
          "column": "_last_modified_time",
          "type_only": true,
          "note": "the cursor IS the object's upload stamp; each run uploads its own copy"
        },
        {
          "column": "_olake_id",
          "type_only": true,
          "note": "hashes the record carrying _last_modified_time"
        }
      ],
      "variants": {
        "csv": {},
        "json": {},
        "parquet": {},
        "xml": {}
      }
    }
  },
  "destinations": {
    "iceberg": {
      "arrow": {
        "min_baseline": "v0.3.17",
        "note": "P1: #531 (v0.3.6) through v0.3.16 arrow integer widths disagree with today's; below v0.3.6 no arrow writer exists"
      }
    },
    "parquet": {
      "skip_baselines": [
        "v0.3.16"
      ],
      "note": "P2: v0.3.16 adds the `data` column unconditionally (#810, fixed by #826 in v0.3.17); below that the no-CDC drivers' output lacks _cdc_timestamp (#810) and oracle's EvolveSchema panics on a widened column (P3, #602)"
    }
  }
}